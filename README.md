## Modrinth extras

This extension add some useful features to the https://modrinth.com website

#### cURL button

Instead of the "open version" button in the download screen there will be a "cURL" button that will copy the curl command to download selected version of that project. This is useful when you need to download a mod using the terminal (for example on a VPS)